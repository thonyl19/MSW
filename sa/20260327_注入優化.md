對於注入功能,我有以下的優化想法,
1. 注入的資料來源,改依據以下程序取得
```
import { _inject } from './_MultOper_MPI.data.js';
```
2.取得資料格式類似如下, 第一層的 key 值, 等同於 target 的值, 第二層的 key/value 值, 等同於 list 每一項 的 text/value
```
{
    form:{
        "一階流程案例":{
            LOT:"JOSEPH_TEST_LAYER-02",
            WO:"JOSEPH_TEST_LAYER",
            OPER_SID:"GTI25092609482676057",
            ROUTE_VER_OPER_SID:"GTI25092610430277229",
            SUB_ROUTE_VER_OPER_SID:null,
            UNIT:"PCS",
            QUANTITY:"20.000",
            EQP_SID:null,
            COMMENT:null,
            IsMustSelectedEqp:false,
            Has_sub_record_oper:false
        },
        "一階流程案例1":{
            
        }
    },
}
```
3.透過一個轉換程序, 將 _inject 的資料轉換成 以下結構 , 再轉由 @/components/MockPanel.js 動態生成 UI
```
{
    target: 'form', 
    type: 'actions',
    list: [
        { text: '一階流程案例', value  },
        { text: '一階流程案例1', value },  
    ]
}
```

此外 ,我希望將 @/components/MockPanel.js 的介面調整為 tabs 的架構
1. 第一個 tab 為 "MSW", 主要處理 MSW 案例情境
2. 第二個 tab 為 "Inject", 主要處理 注入功能
