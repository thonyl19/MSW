---
description: 自主代理執行工作流
---

> [!NOTE]
> 本工作流專為「全自動開發與自我修復」設計。當任務涉及複雜邏輯且有自動化測試支援時，應優先使用此模式。

## 核心模式：Observe-Think-Fix-Verify (OTFV)

本工作流賦予 Agent 權限，在遇到執行失敗（如測試未通過、編譯錯誤）時，不需立即中斷，而應自動進行「日誌分析 -> 回歸修復 -> 再次驗證」，直到達成目標或觸發熔斷。

---

## Step 0: 任務初始化與錨點同步 (Initial)
> [!IMPORTANT]
> 執行前必須依據 [/sync-SOP] 進行自我查核。
1.  **讀取任務**：閱讀當前的 `{task-slug}.md`。
2.  **宣告計劃**：輸出符合 `GEMINI.md` 的 Execution Plan，並標註 `Mode: Autonomous Agent`。

---

## Step 1: 實作與安全備份 (Implementation)
1.  **物理相位隔斷 (或 封裝執行)**：
    *   **方式 A (推薦)**：調用 `run_command` 將「備份腳本」與「異動邏輯」**封裝在同一個 PowerShell 腳本塊**中執行。
        *   腳本內部必須確保：`Copy-Item` 成功後才執行後續異動。
        *   此方式僅需使用者點擊 **一次** 核准。
    *   **方式 B (傳統)**：先調用 `run_command` 備份，取得 DONE 後在下一回合執行寫入。
2.  **自動化標籤**：針對具備備份保護的指令，應標記 `SafeToAutoRun: true`。

---

## Step 2: 自主修復迴圈 (Autonomous Healing Loop)
// turbo
1.  **執行驗證**：運行測試指令 (如 `node --test` 或 `npm run dev`)。
2.  **成功路徑**：若 Exit Code == 0 -> 跳轉至 Step 3。
3.  **修復分支 (Recursive Fix)**：
    *   **分析**：讀取錯誤日誌，定義失敗類型。
    *   **深度控制**：最大重試次數為 **3 次**。
    *   **追蹤記錄**：在 `task.md` 更新 Attempt 紀錄。
    *   **修正執行**：回到 Step 1 (必須先備份修補前的版本，再進行修正寫入)。
    *   **再次驗證**：回到 Step 2.1。

---

## Step 3: 任務結算與記錄 (Finalization)
1.  **更新日誌**：在 `task.md` 標註 `[x]` 或 `Success`。
2.  **結算報告**：輸出動作摘要，並明確指出是否有觸發「自動修復」。
3.  **歸檔備份**：確保所有產出的臨時或中繼備份檔都已妥善留存於檔案系統中。

---

## 🚫 熔斷條件 (Circuit Breaker)
發生以下情境時，必須 **STOP** 執行並請示用戶：
*   自動修復次數超過 3 次。
*   錯誤涉及「權限不足 (Access Denied)」或「硬體/路徑缺失」。
*   修復邏輯可能導致「資料遺失」或「破壞性變更」。
